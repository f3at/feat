[application:featchat]
import: featchat.everything
name: featchat

[host]
ports: chat:10000:10010

# uncomment lines below for the first machine in your system
# [agent:alert]
# agent_type: alert_agent

# [agent:api]
# agent_type: api_agent

# [agent:dns]
# agent_type: dns_agent
# descriptor.suffix: some.suffix.lan
# initiate.slaves: [[ip1, port1], [ip2, port2], ...]

